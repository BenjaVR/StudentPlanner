{
  "name": "shared",
  "version": "0.1.0",
  "devDependencies": {
    "del-cli": "^1.1.0",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "build": "del-cli ./dist && tsc -d",
    "build:watch": "del-cli ./dist && tsc -d -w"
  }
}
